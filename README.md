# Stellaris-Script-Formatter
A tool to convert Stellaris script between human-readable format (with indentation, comments, line breaks) and compact format (required by game engine). 一个用于在可读格式（带缩进、注释、换行）和紧凑格式（游戏引擎要求的格式）之间相互转换群星脚本的工具。
